meta {
  name: List Unsolved Captchas
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/captcha-labels/unsolved
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200 && res.body.length > 0) {
    bru.setVar("captchaObjectName", res.body[0].object_name);
  }
}

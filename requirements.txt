fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.12   # required by FastAPI's Form(...) — see app/api/v1/label_ui.py
celery==5.4.0
flower==2.0.1
redis==5.0.8
# pyautogui==0.9.54   # legacy PyAutoGUI/Xvfb RPA path — commented out in favor of nodriver (see app/tasks/rpa_tasks.py)
# pytesseract==0.3.13
# pillow==10.4.0
pydantic-settings==2.5.2
nodriver==0.45.2
minio==7.2.9
requests==2.32.3

# Shared with requirements-training.txt: app/core/captcha_labels/ (used by
# both rpa_tasks.py's record_captcha and training_tasks.py's
# fetch_labeled_captchas) is backed by SQLAlchemy over psycopg, so every
# image that imports app.core.captcha_labels needs both — not ML-specific,
# just this table's driver.
psycopg[binary]==3.2.3
sqlalchemy==2.0.35
